schema: http://localhost:5001/graphql
overwrite: true
documents: ./src/api/**/*.ts
generates:
  ./src/types/types.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-react-apollo
